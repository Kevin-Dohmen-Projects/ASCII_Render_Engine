name: Build and Release

on:
  # push:
  #   branches:
  #     - master
  # pull_request:
  #   branches:
  #     - master
  workflow_dispatch:

jobs:
  build-publish:
    uses: Kevin-Dohmen-Projects/pipelines-generic/.github/workflows/dotnet-libraries.yml@main
    with:
      solutionPath: "CSharp_ASCII_Render_Engine.sln"
      skipTests: true
  