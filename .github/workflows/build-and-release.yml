name: Build and Release

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  workflow_dispatch:

jobs:
  build-publish:
    uses: Kevin-Dohmen/pipelines-generic/.github/workflows/dotnet-libraries.yml@c0cebaaf8260a90face023fcc03e83d25e8c64ec
    with:
      solutionPath: 'src/MyLibrary.sln'
      skipTests: true
  